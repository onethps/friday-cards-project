import React, { FC } from 'react';

import style from 'components/common/Preloader/Preloader.module.scss';

export const Preloader: FC = () => <div className={style.preloader} />;

export default Preloader;
